{
  "compilerOptions": {
    "target": "es2022",
    "module": "commonjs",

    "rootDir": "./src", // set root dir for source files
    "baseUrl": "./src", // Change this to "./src"
    "outDir": "./dist", // set output dir for build files
    "paths": {
      // paths resolvers
      "@/*": ["./*"]
    },

    "declaration": true,
    "removeComments": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "sourceMap": true,
    "incremental": false, // incremental build to speed up the build process
    "skipLibCheck": true, // ignore node_modules
    "strictNullChecks": true, // allow null checks for Zod infers to work
    "noImplicitAny": false, // allow any type
    "strictBindCallApply": false, // allow bind, call and apply to work
    "forceConsistentCasingInFileNames": false, // allow consistent casing in file names
    "noFallthroughCasesInSwitch": false, // allow fallthrough cases in switch
    "resolveJsonModule": true, // allow import json
    "esModuleInterop": true // allow import es modules
  },

  "exclude": ["node_modules", "dist"]
}
